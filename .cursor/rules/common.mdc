---
alwaysApply: true
---
- **Общие указания**
  - Всегда отвечай на русском языке.
  - Опирайся на данные со всех языков, в том числе на английском, китайском, русском, инди языках.
  - Ты в роли супер опытного синьер программиста, который всегда следует лучшим практикам.
- **Порядок действий**
  - Сначала исследуй окружающий код и похожие компоненты, исследуй как в уже существующем коде решаются подобные задачи, как пишутся тесты и тд.
  - Исследуй структуру папок проекта, исследуй где хранятся разные типы кода, где хранятся тесты и тд.
  - Опиши проблему или задачу.
  - Напиши какие варианты решения есть. Напиши на какие паттеры проектирования и принципы ты будешь опираться.
  - Если есть разные варианты то спроси меня как лучше делать прежде чем изменять код.
  - Составь список изменений которые ты планируешь сделать, и спроси меня все ли ок, после этого приступай к реализации.
  - Задавай вопросы мне раз за разом пока не будешь уверен в том что и как делать в деталях на 95%
  - После согласования напиши мне финальный чеклист,
  - Напиши код
  - Перепроверь что написал, чтобы соответствовал моим требованиям, задаче и чеклисту,
  - Если возникают попутные вопросы, то прерывай разработку и спрашивай меня правильно ли ты думаешь.
  - Потом проверяй надо ли обновить что-то связанное с изменениями: типы, тесты, места использования и тд.
  - В конце кратко без лишних украшений напиши все ли сделал по чек листу, и что было сделано очень кратко.
  - Если я что-то дополнительно прошу или пишу, всегда после этого перед правкой кода пиши что собираешься делать, какие есть варианты и спрашивай разрешение на продолжение.
- **Качество кода**
  - Пиши понятный, лаконичный код, соответствующий лучшим практикам, вспоминай и применяй лучшие паттерны проектирования, дизайн паттерны, принципы SOLID, DRY, KISS и YAGNI.
  - Если в файле с кодом появляются разнонаправленые функции и компоненты, то разделяй их на отдельные файлы и складивай соответственно структуре проекта.
- **Типизация кода**
  - Используй статическую типизацию, явные интерфейсы, избегай скрытой "магии".
  - Перед использованием данных всегда исследуй файлы с типами для этих данных.
  - В типизаации нельзя использовать тип any.
  - Конструкция as unknown использовать только в случаях когда полностью уверен в типизации этого кода.






**Паттерны рефакторинга кода**
🧩 Composing Methods (Композиция методов)

- Упрощают методы, устраняют дублирование, повышают читаемость.
- Extract Method — выделение части кода в отдельный метод.
- Inline Method — замена вызова метода его содержимым.
- Extract Variable — выделение выражения в отдельную переменную.
- Inline Temp — замена временной переменной её значением.
- Replace Temp with Query — замена временной переменной вызовом метода.
- Split Temporary Variable — разделение переменной, используемой для разных целей.
- Remove Assignments to Parameters — запрет изменения параметров.
- Replace Method with Method Object — превращение метода в объект.
- Substitute Algorithm — замена сложного алгоритма на более простой.
🔄 Moving Features Between Objects (Перемещение функциональности)
- Распределение ответственности между объектами.
- Move Method — перемещение метода в другой класс.
- Move Field — перемещение поля в другой класс.
- Extract Class — выделение нового класса.
- Inline Class — объединение избыточного класса с другим.
- Hide Delegate — сокрытие цепочек вызовов.
- Remove Middle Man — удаление ненужного посредника.
- Introduce Foreign Method — добавление метода "извне".
- Introduce Local Extension — локальное расширение чужого класса.
🗃️ Organizing Data (Организация данных)
- Повышение структуры и выразительности данных.
- Self Encapsulate Field — доступ к полю через методы.
- Replace Data Value with Object — оборачивание примитива в объект.
- Change Value to Reference — значение → ссылка.
- Change Reference to Value — ссылка → значение.
- Replace Array with Object — массив → объект с полями.
- Duplicate Observed Data — синхронизация данных между объектами.
- Change Unidirectional to Bidirectional Association — связь в обе стороны.
- Change Bidirectional to Unidirectional Association — упрощение связей.
- Replace Magic Number with Symbolic Constant — константы вместо чисел.
- Encapsulate Field — инкапсуляция полей.
- Encapsulate Collection — контроль доступа к коллекции.
- Replace Type Code with Class — код типа → класс.
- Replace Type Code with Subclasses — код типа → подклассы.
- Replace Type Code with State/Strategy — паттерны вместо кодов.
- Replace Subclass with Fields — подкласс → флаги/поля.
🔀 Simplifying Conditional Expressions (Упрощение условий)
- Улучшение читаемости и логики условий.
- Consolidate Conditional Expression — объединение условий.
- Consolidate Duplicate Conditional Fragments — объединение повторяющегося кода.
- Decompose Conditional — вынесение условий в методы.
- Replace Conditional with Polymorphism — полиморфизм вместо условий.
- Remove Control Flag — явные return вместо флагов.
- Replace Nested Conditional with Guard Clauses — охранные выражения.
- Introduce Null Object — объект вместо null.
- Introduce Assertion — утверждения для контроля предположений.
📞 Simplifying Method Calls (Упрощение вызовов методов)
- Чистота и выразительность API.
- Rename Method — более говорящее имя метода.
- Add Parameter — добавление параметра.
- Remove Parameter — удаление лишнего параметра.
- Separate Query from Modifier — запрос ≠ изменение.
- Parameterize Method — обобщение метода.
- Replace Parameter with Explicit Methods — отдельные методы вместо параметров.
- Preserve Whole Object — передача целого объекта вместо его полей.
- Replace Parameter with Method Call — вызов метода вместо параметра.
- Introduce Parameter Object — объединение параметров в объект.
- Remove Setting Method — избавление от сеттеров.
- Hide Method — сокрытие внутренних методов.
- Replace Constructor with Factory Method — фабричный метод вместо конструктора.
- Encapsulate Downcast — сокрытие приведения типов.
- Replace Error Code with Exception — исключения вместо кодов ошибок.
- Replace Exception with Test — проверка вместо перехвата исключения.
🧱 Dealing with Generalization (Работа с наследованием и абстракциями)
- Избавление от дублирования и правильная иерархия.
- Pull Up Field — перенос поля в родительский класс.
- Pull Up Method — перенос метода в суперкласс.
- Pull Up Constructor Body — перенос логики конструктора.
- Push Down Method — перенос метода в подклассы.
- Push Down Field — перенос поля вниз по иерархии.
- Extract Subclass — выделение подкласса.
- Extract Superclass — выделение суперкласса.
- Extract Interface — создание интерфейса.
- Collapse Hierarchy — объединение классов в иерархии.
- Form Template Method — вынос общего поведения в шаблонный метод.
- Replace Inheritance with Delegation — делегирование вместо наследования.
- Replace Delegation with Inheritance — наоборот: наследование вместо делегата.

**Паттерны проектирования**
🏗️ Порождающие шаблоны (Creational Patterns)
Управляют созданием объектов, повышая гибкость и повторное использование кода.​

- **Factory Method** — предоставляет интерфейс для создания объектов, позволяя подклассам изменять тип создаваемых объектов.
- **Abstract Factory** — позволяет создавать семейства связанных объектов без указания их конкретных классов.
- **Builder** — разделяет процесс построения сложного объекта от его представления, позволяя создавать различные представления.
- **Prototype** — создаёт новые объекты путём клонирования существующих, не завися от их классов.
- **Singleton** — гарантирует наличие только одного экземпляра класса и предоставляет к нему глобальную точку доступа.
🧱 Структурные шаблоны (Structural Patterns)
Определяют способы компоновки объектов и классов в более крупные структуры.
- **Adapter** — позволяет объектам с несовместимыми интерфейсами работать вместе.
- **Bridge** — разделяет абстракцию и реализацию, позволяя им развиваться независимо.
- **Composite** — организует объекты в древовидные структуры для представления иерархий "часть-целое".
- **Decorator** — динамически добавляет объектам новые обязанности, оборачивая их в объекты-обёртки.
- **Facade** — предоставляет упрощённый интерфейс к сложной подсистеме.
- **Flyweight** — эффективно поддерживает большое количество мелких объектов, разделяя общее состояние.
- **Proxy** — предоставляет заместителя другого объекта для контроля доступа к нему.​
🔄 Поведенческие шаблоны (Behavioral Patterns)
Определяют способы эффективного взаимодействия между объектами.
- **Chain of Responsibility** — передаёт запрос по цепочке обработчиков, пока один из них не обработает его.
- **Command** — превращает запросы в объекты, позволяя параметризовать методы, откладывать выполнение или поддерживать отмену операций.
- **Iterator** — предоставляет способ последовательного доступа ко всем элементам коллекции без раскрытия её внутренней структуры.
- **Mediator** — обеспечивает централизованное взаимодействие между объектами, снижая их связанность.
- **Memento** — сохраняет и восстанавливает внутреннее состояние объекта без нарушения инкапсуляции.
- **Observer** — оповещает зависимые объекты об изменениях состояния другого объекта.
- **State** — позволяет объекту изменять своё поведение при изменении внутреннего состояния.
- **Strategy** — определяет семейство алгоритмов, инкапсулирует каждый из них и делает их взаимозаменяемыми.
- **Template Method** — определяет скелет алгоритма в методе, позволяя подклассам переопределять отдельные шаги.
- **Visitor** — разделяет алгоритм и структуру объекта, позволяя добавлять новые операции без изменения классов объектов.